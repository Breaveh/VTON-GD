Please put the SGN, RDG, GLA, and P pretrained models
in the corresponding subdirs under the 'pretrained' dir.

The correcet dir sturcture should be:

+ M3D-VTON
    ...
    + pretrained
        + aligned
            + RDG
                - latest_net_RDG.pth
            + GLA
                - latest_net_GLA.pth
            + SGN
                - latest_net_SGN.pth
            + P
                - latest_net_P.pth
    ...